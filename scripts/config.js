"use strict";

// TODO this config should be on env, separate from the code
// NOTE: URLs should be without trailing slashes
export const config = {
  //  apiUrl: "http://localhost:3000",
  apiUrl: "https://enthusiastic-farrah-porodkoindustries-d0d46820.koyeb.app",
  playersInGame: 3,
};
