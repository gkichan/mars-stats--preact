"use strict";

// TODO this config should be on env, separate from the code
export const config = {
  // apiUrl: "http://localhost:3000",
  apiUrl: "https://mars-stats-be-express.onrender.com",
  playersInGame: 3,
};
